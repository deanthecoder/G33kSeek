# Agents.md — Guidance for AI-assisted Development
_An overview of the current repository and its related projects, generated by [AgentPrimer](https://github.com/deanthecoder/AgentPrimer)._

## Summary for Agents

Use this overview as factual context when generating or editing code.  
Avoid unnecessary token use; prefer referencing this document instead of re‑parsing source files.

G33kSeek is a **cross‑platform keyboard launcher** similar to macOS Spotlight.  
It allows users to quickly search files, launch applications, run commands, perform calculations, search the web, and send prompts to AI services.

The UI is built with **Avalonia** and the codebase is written in **C# (.NET)**.

---

# Agent Instructions (Overview)

- Read this document before proposing architectural changes.
- Follow the repository’s naming conventions and coding style.
- Prefer extending existing providers/services rather than creating new patterns.
- Prefer reusing code from **DTC.Core** and other existing deanthecoder repositories before adding new local helpers.
- If a new piece of functionality is generic and reusable, note that it may belong in **DTC.Core** rather than this repository.
- Reuse proven code from **G33kShell** where appropriate, especially for file content searching / grepping.
- New behaviour should normally be covered by unit tests.
- Keep solutions **simple and performant** — this application must feel instant.
- Avoid heavy dependencies unless clearly justified.
- Git commit messages must start with `Feature:`, `Fix:`, or `Other:`, use sentence case, and end with a full stop.
- Persist launcher settings via `DTC.Core.Settings.UserSettingsBase` rather than inventing a separate settings persistence layer.
- Prefer method names without underscores. Async methods should end with `Async` unless a framework contract makes that impossible.
- Ensure new public classes and public methods have unit tests unless that would be inappropriate or require disproportionate setup for very little gain.
- Validate public method arguments appropriately, including null checks, range checks, and similar guard clauses where relevant.
- If a nearby change also includes small incidental cleanups such as removing unused `using` directives or suppressing an existing warning, it is fine to fold those into the same commit rather than splitting them out.

---

# Architectural Overview

G33kSeek is built around a **query provider system**.

User input is interpreted and routed to a provider responsible for generating results.

Example:

```
QueryEngine
   ├── AppSearchProvider
   ├── FileSearchProvider
   ├── CalculatorProvider
   ├── WebSearchProvider
   ├── ContentSearchProvider
   ├── AiPromptProvider
   └── CommandProvider
```

Providers should:

- Accept a query string
- Return ranked results
- Execute an action when selected

Providers should remain **loosely coupled** so new functionality can be added easily.

---

# 
# Project Structure

Agents should prefer placing new code in the following folders so the repository remains organised and predictable.

```
/Providers
/Services
/Views
/ViewModels
/Commands
/Models
```

Guidelines:

- **Providers** implement query functionality (calculator, file search, AI prompts, etc).
- **Services** contain reusable logic (indexing, search orchestration, AI communication, etc).
- **Views** contain Avalonia `.axaml` UI definitions.
- **ViewModels** implement the UI logic using MVVM.
- **Commands** contain executable actions triggered from results.
- **Models** contain simple data structures used by providers and services.

Avoid scattering classes across the repository.  
If unsure where something belongs, prefer **Services** or **Providers**.

---

# Query Prefix System

The first character(s) of the query determines the provider.

| Prefix | Mode | Example |
|------|------|------|
| *(none)* | File/App search | `rider` |
| `=` | Calculator | `=2+2` |
| `?` | Help / examples | `?` or `?calc` |
| `??` | File content search | `??TODO` |
| `@` | AI prompt | `@summarise this text` |
| `>` | Commands | `>shutdown` |

When no prefix is used, the launcher performs **file and application search**.

Current UX decisions:

- A bare `?` should open **in-app help** rather than web search.
- Typing after `?` can be used to **filter help topics** for now.
- `@` remains reserved for **AI prompts**, not general web search.
- Typing `http://`, `https://`, or `www.` should eventually be recognised as a **direct URL intent** and open in the browser.
- When the search box is empty, the launcher should hint at examples such as:
  - no prefix for app/file search
  - `=2+2`
  - `?` for help
  - `>` for commands
- For macOS application search, a fast top-level scan of `/Applications` and `~/Applications` is preferred.
- macOS app results can use the `.app` bundle name directly as the display name; no deeper bundle inspection is required for the first slice.

---

# Core UI Behaviour

The launcher window should:

- Appear when a **global hotkey** is pressed.
- Focus the search textbox automatically.
- Update results **as the user types**.
- Allow keyboard navigation of results.
- Close automatically after executing a result.

Avoid unnecessary UI complexity.

This tool should remain **minimal and extremely responsive**.

---

# Global Hotkey Implementation

Global keyboard shortcuts should be implemented using the **SharpHook** library.

SharpHook provides reliable cross‑platform global keyboard hooks and works well with .NET and Avalonia.

Agents should prefer **SharpHook** rather than implementing platform‑specific keyboard hooks.

Example responsibilities:

- Register a global launcher shortcut (e.g. `Ctrl+Space` or user configurable).
- Trigger the launcher window when the shortcut is pressed.
- Ensure the hook runs off the UI thread where appropriate.

Keep the implementation lightweight and avoid blocking the hook thread.

---

# Tray Icon

The application should provide a system tray icon for:

- Enabling/disabling the launcher
- Opening settings
- Exiting the application

Avalonia supports cross‑platform tray icons.

---

# Performance Principles

G33kSeek should feel instant.

This repository can benefit from existing fast file-content search code from **G33kShell**. Agents should prefer adapting or reusing that proven grepping/search logic rather than inventing a new implementation from scratch.

Agents should prefer:

- asynchronous operations
- incremental searching
- lightweight indexing

Avoid:

- blocking UI thread work
- expensive disk scans during queries
- unnecessary allocations

---

# DTC.Core Usage

This project includes **DTC.Core** as a submodule and agents should prefer using its helpers rather than re‑implementing functionality.

Agents should also consider whether new generic helpers belong in **DTC.Core** instead of G33kSeek. If code is broadly reusable and not specific to launcher behaviour or Avalonia UI, it is a good candidate for **DTC.Core**.

File-system objects should usually be represented as **FileInfo** and **DirectoryInfo** rather than raw strings.

Agents should prefer:

```
FileInfo
DirectoryInfo
DTC.Core extension methods
TempFile
```

over passing file-system paths around as plain strings where a richer type is sensible.

---

# Query Engine Decisions

- Prefixes route queries, but **not every mode uses a prefix**:
  - no prefix = default app/file search
  - `=` = calculator
  - other prefixes route to specialist providers as they are implemented
- Providers should return:
  - a list of visible results
  - optional status text for the launcher UI
- A query result should carry its **default Enter action** so the UI can stay generic.
- Some providers may return a **single output value**:
  - example: calculator
  - pressing **Enter** should copy that value to the clipboard
- Some providers may return a **refinable list**:
  - example: file search or content search
  - pressing **Enter** may open an item or copy a path depending on provider intent
- Design provider/result interfaces so both “single computed value” and “filtered result list” fit the same pipeline.

Useful helpers include:

File system helpers:

```
string.ToDir()
string.ToFile()
dir.GetDir("a/b")
dir.GetFile("x.txt")
```

Safe enumeration:

```
DirectoryInfo.TryGetDirs(...)
DirectoryInfo.TryGetFiles(...)
DirectoryInfo.TryGetContent(...)
```

Deletion:

```
DirectoryInfo.TryDelete()
FileInfo.TryDelete()
FileSystemInfo.TryDelete()
```

Opening items:

```
FileInfo.OpenWithDefaultViewer()
Uri.Open()
```

Existence checks:

```
fileInfo.Exists()
directoryInfo.Exists()
```

---

# XML Documentation Pattern

Add class‑level XML documentation for reusable types.

Example:

```
<summary>
Provides application search functionality for G33kSeek.
</summary>

<remarks>
Used by the QueryEngine when no prefix is provided.
</remarks>
```

Guidelines:

- `<summary>` explains what the class does.
- `<remarks>` explains why it exists in the project.
- Keep documentation concise.

---

# Coding Style

Agents should follow these conventions:

- Language: **C#**
- Framework: **.NET**
- UI: **Avalonia**
- Tests: **NUnit**
- File-system paths: prefer **FileInfo** / **DirectoryInfo** over `string` where sensible

General rules:

- Prefer readable code over clever code.
- Use clear method and variable names.
- Avoid unnecessary abstractions.
- Follow existing repository patterns.
- Prefer using existing extension methods and helper types over duplicating path-handling code.
- Consider whether reusable non-UI code should be moved into **DTC.Core**.
- Add or update unit tests for new behaviour.

---

# ReviewG33k-Friendly Defaults

These are lightweight habits that help keep ReviewG33k findings low without over-optimising for any single tool.

- Keep files focused and avoid duplicate helper code where an existing service or extension method already fits.
- Prefer simple MVVM boundaries: keep view logic in views, state/commands in view-models, and reusable logic in services/providers.
- Avoid unused `using` directives, dead private members, and one-off fields that can just be locals.
- Prefer `readonly` for fields that are only assigned during construction.
- Keep async code honest:
  - prefer `Task` / `Task<T>` over `async void`
  - use the `Async` suffix for async methods unless a framework/interface contract prevents it
  - avoid wrapping async work in `Task.Run(...)` unless there is a clear need
- When writing numeric values to files or persisted text, prefer invariant culture formatting.
- Add class-level XML docs to reusable public types:
  - `<summary>` = what it does
  - `<remarks>` = why it is useful here
- Keep Avalonia bindings tidy:
  - prefer typed bindings/context hints where practical
  - avoid unnecessary wrapper panels/containers
- Preserve clean formatting:
  - blank lines between methods
  - no stray blank lines between consecutive brace-only lines
- If behaviour changes, update tests. If the user-facing workflow changes noticeably, also update the README.

---

# Future Extensions

G33kSeek is expected to evolve. Future capabilities may include:

- Clipboard history
- Emoji picker
- Unit conversions
- Colour utilities
- Git commands
- Plugin system
- Support for local AI models

Agents should design new systems so they remain **extensible**.

---

# Project Philosophy

G33kSeek should remain:

- fast
- simple
- keyboard‑driven
- developer‑friendly
- easily extensible

If a feature makes the launcher feel heavy or slow, reconsider the design.
