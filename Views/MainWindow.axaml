<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:G33kSeek.Models"
        xmlns:viewModels="clr-namespace:G33kSeek.ViewModels"
        mc:Ignorable="d"
        x:Class="G33kSeek.Views.MainWindow"
        x:DataType="viewModels:MainWindowViewModel"
        Width="760"
        Height="560"
        MinWidth="760"
        MinHeight="560"
        MaxWidth="760"
        MaxHeight="560"
        Background="Transparent"
        BorderBrush="Transparent"
        BorderThickness="0"
        CanResize="False"
        Icon="/Assets/app.ico"
        Padding="0"
        ShowInTaskbar="False"
        TransparencyBackgroundFallback="Transparent"
        TransparencyLevelHint="Transparent"
        WindowStartupLocation="Manual"
        SystemDecorations="None"
        Topmost="True"
        Title="G33kSeek">
  <Design.DataContext>
    <viewModels:MainWindowViewModel />
  </Design.DataContext>

  <Border Classes="launcherShell"
          Background="#E0000000"
          ClipToBounds="True"
          Margin="0"
          Padding="0"
          VerticalAlignment="Top">
    <Grid>
      <Border>
        <Border.Background>
          <ImageBrush Source="/Assets/main-window-bg.jpg"
                      Opacity="0.25"
                      AlignmentY="Top"
                      Stretch="None" />
        </Border.Background>
      </Border>

      <Grid Margin="18"
            RowDefinitions="Auto,Auto,Auto">
        <Grid Margin="0,0,0,14"
              ColumnDefinitions="*,Auto">
          <StackPanel Spacing="6">
            <TextBlock Classes="launcherEyebrow"
                       Text="KEYBOARD LAUNCHER" />
            <TextBlock Classes="launcherTitle"
                       Text="G33kSeek" />
            <TextBlock Classes="launcherHint"
                       Text="{Binding StatusText}" />
          </StackPanel>

          <ProgressBar Grid.Column="1"
                       Width="52"
                       Height="4"
                       Margin="12,2,0,0"
                       VerticalAlignment="Top"
                       IsIndeterminate="True"
                       IsVisible="{Binding IsRefreshActive}"
                       Foreground="#E6F5F5F5"
                       Background="#33FFFFFF"
                       ShowProgressText="False" />
        </Grid>

        <Border Grid.Row="1"
                Classes="searchSurface">
          <TextBox x:Name="SearchTextBox"
                   Background="Transparent"
                   BorderThickness="0"
                   Classes="searchBox"
                   FontSize="24"
                   Text="{Binding SearchText, Mode=TwoWay}"
                   Watermark="Type to search files, apps, commands, or the web..." />
        </Border>

        <Border Grid.Row="2"
                Margin="0,14,0,0"
                MaxHeight="{Binding ResultsPanelMaxHeight}"
                Opacity="{Binding ResultsPanelOpacity}"
                IsVisible="{Binding HasResults}"
                Classes="resultsPanel">
          <Border.Transitions>
            <Transitions>
              <DoubleTransition Property="MaxHeight"
                                Duration="0:0:0.18" />
              <DoubleTransition Property="Opacity"
                                Duration="0:0:0.12" />
            </Transitions>
          </Border.Transitions>
          <ListBox x:Name="ResultsListBox"
                   Background="Transparent"
                   BorderThickness="0"
                   DoubleTapped="OnResultsListBoxDoubleTappedAsync"
                   ItemsSource="{Binding Results}"
                   ScrollViewer.VerticalScrollBarVisibility="Auto"
                   SelectedItem="{Binding SelectedResult, Mode=TwoWay}">
            <ListBox.ItemTemplate>
              <DataTemplate x:DataType="models:QueryResult">
                <Grid ColumnDefinitions="*,Auto"
                      Background="Transparent"
                      Margin="2"
                      PointerPressed="OnResultPointerPressed">
                  <Grid.ContextMenu>
                    <ContextMenu ItemsSource="{Binding SecondaryActions}">
                      <ContextMenu.ItemTemplate>
                        <DataTemplate x:DataType="models:QueryActionDescriptor">
                          <MenuItem Header="{Binding DisplayText}"
                                    Click="OnSecondaryActionMenuItemClickAsync" />
                        </DataTemplate>
                      </ContextMenu.ItemTemplate>
                    </ContextMenu>
                  </Grid.ContextMenu>
                  <StackPanel Spacing="2">
                    <TextBlock FontSize="16"
                               FontWeight="SemiBold"
                               Text="{Binding Title}" />
                    <TextBlock Classes="resultSubtitle"
                               Text="{Binding Subtitle}" />
                  </StackPanel>
                  <TextBlock Grid.Column="1"
                             VerticalAlignment="Center"
                             Classes="resultPrefix"
                             Text="{Binding TrailingText}" />
                </Grid>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </Border>
      </Grid>
    </Grid>
  </Border>
</Window>
